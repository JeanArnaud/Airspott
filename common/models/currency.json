{
    "name": "Currency",
    "base": "PersistedModel",
    "description": "A Currency represents a real world currency which may be related to other Models",
    "idInjection": true,
    "options": {
        "validateUpsert": true
    },
    "properties": {
        "identifier": {
            "type": "string",
            "required": true,
            "description": "Identifier for the currency (EUR, USD, etc.)"
        },
        "symbol": {
            "type": "string",
            "required": true,
            "description": "Symbol for the currency (€, $, etc.)"
        }
    },
    "validations": [],
    "relations": {},
    "acls": [],
    "methods": {}
}
